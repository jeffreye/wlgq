#include "query.h"

class sync_query
{
    public:
        sync_query();
        ~sync_query();
    protected:

    private:

};

class sync_query_operator:public query_operator
{
    public:
        sync_query_operator();
        ~sync_query_operator();
    protected:

    private:

};
